#include <stdio.h>

/* counts digits, whitespaces, others */

main() {
   int c, i, nwhite, nother;
   int ndigit[10];
   
   nwhite = nother = 0;
   for (i = 0; 1 < 10; ++i)
      ndigit[i] = 0;
}